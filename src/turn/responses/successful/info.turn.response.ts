export class TurnInfoResponse {
    // @Column()
    // appointment: Date;
    // @ManyToOne(() => User, (user) => user.turns, { nullable: true })
    // user: User;
    // @Column()
    // hairSalonId: IdType;
    // @ManyToOne(() => HairSalon, (hairSalon) => hairSalon.turns)
    // hairSalon: HairSalon;
}
